<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.silenthelp">

    <!-- Modified by Kelley Rosa 06-10-25 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name=".core.SilentHelpApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SilentHelp"
        tools:targetApi="31">


        <!-- Modified by Kelley Rosa 05-22-25 -->
        <activity android:name=".ui.keyword.KeywordSettingsActivity" />
        <!-- Modified by Kelley Rosa 06-07-25 -->
        <activity android:name=".ui.keyword.EditKeywordsActivity" />
        <activity android:name=".ui.fakecall.FakeCallActiveActivity"/>




        <!-- Modified by Carmelo Vera 06-02-25 -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.SilentHelp">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Modified by Carmelo Vera 06-05-25 -->
        <activity android:name=".ui.fakecall.FakeCallActivity" />

        <!-- Modified by Carmelo Vera 06-05-25 -->
        <!-- Colin – User Settings -->
        <activity android:name=".ui.user.UserSettingsActivity" />

        <!-- Michael – Incident Log -->
        <activity android:name=".ui.incident.IncidentLogActivity" />



        <activity android:name=".ui.home.MainActivity" />
    </application>

</manifest>
